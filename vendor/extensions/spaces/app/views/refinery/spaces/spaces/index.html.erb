<% content_for :body do %>
  <ul id="spaces">
    <% @spaces.each do |space| %>
      <li>
        <%= link_to space.owner, refinery.spaces_space_path(space) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
